import React from 'react';
import { Account } from './Account';
import App from './App';

const Wrapper = () => {
  return (
    <Account>
      <App />
    </Account>
  );
};

export default Wrapper;
